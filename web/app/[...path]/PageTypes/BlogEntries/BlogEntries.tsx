import React from 'react';
import { PageBaseType } from '../../../types/PageType';

export default function BlogEntries (
    {
        pageData,
    }: {
        pageData: PageBaseType;
    },
) {
    console.log(pageData);

    return <>TODO Blog Entries</>;
}
